;; ~/.aws/config
;; template for AWS CLI v2

[default]
region = ap-northeast-1
output = json
; output = text
; output = yaml

;; for sso login session
;; `aws sso login --profile COM_PRD`
;; `aws sts get-caller-identity --profile COM_PRD`
[sso-session AWS_SESSION]
output = json
sso_region = ap-northeast-1
sso_registration_scopes = sso:account:access
;; change start_url
sso_start_url = https://x-xxxxxxxxx.awsapps.com/start

[profile SAMPLE_PROFILE]
output = json
region = ap-northeast-1
sso_session = AWS_SESSION
sso_account_id = 123456789012
sso_role_name = ps-org-admin-for-snd-dev
